[build]
  command = "pnpm build"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"
  # Next.js runtime plugin handles serverless builds; no static export.

# Cron jobs are now handled by Vercel Cron (see vercel.json).
# The web.koinaku.com domain is served by Vercel; this site remains available
# as a fallback via its Netlify default domain.
