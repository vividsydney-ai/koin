# Session 2026-07-08 — Loop Engineering Setup

## Scope

Build an end-to-end loop engineering setup that Claude Code, Codex, and Kimi Code can use for Koin web MVP and later native/hybrid beta.

## Added

- `docs/agents/LOOP_ENGINEERING.md` with article takeaways, blind spots, Koin-specific operating model, web/native sequencing, parallelism rules, gates, and escalation boundaries.
- `.loop/state.md` and role prompts for orchestrator, maker, and checker.
- `scripts/agent-loop.sh` to print or run a bounded one-task loop through Codex, Claude, or Kimi.
- `scripts/verify-loop.sh` to enforce repo-specific loop checks.
- Package scripts for prompt generation, per-agent runs, and loop verification.
- Optional Claude Stop hook gated by `.loop/require-stop-gate`.
- `.worktreeinclude` and ignore rules for worktree/run-log hygiene.

## Verification

- `bash -n scripts/agent-loop.sh && bash -n scripts/verify-loop.sh && bash -n .claude/hooks/loop-stop-gate.sh` passed.
- `npm run loop:prompt` generated a prompt selecting the next unchecked `TASKS.md` item.
- `npm run verify:loop` passed.

## Notes

- Current branch at setup time: `web-koinaku`.
- The loop runner discovers the active branch at runtime and warns agents not to trust stale branch names in older handoff files.
- Native iOS/Android beta remains paused until the web MVP gates pass and the human explicitly restarts native work.
