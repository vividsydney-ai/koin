# Session 2026-07-18 — KO-LESSON-003

## Goal
Fix three live bugs reported after the last deploy:
1. Replay lesson still shows green XP earned UI.
2. "We couldn't save your progress" error when finishing the next lesson.
3. English/Indonesian lingo mixing when locale is set to Indonesian.

## What shipped
- Replay completion now renders a neutral "Already earned" / 0 XP tile.
- `completeLesson` retries once on RPC failure and synthesizes a replay result if the lesson is already completed, preventing the learner from getting stuck.
- Added Indonesian content columns (`summary_id`, `concept_body_id`, etc.) and populated translations for lessons 1-32.
- `LessonPlayer` uses Indonesian fields when `locale === "id"`; source titles fall back to `local_title`; example/simpler/quiz alternate actions are hidden in ID until translated variants exist.
- Auth sign-in only sends captcha `options` when a token is present.

## Commits
- `9645551` fix(lessons): KO-LESSON-003 replay XP UI, save-progress resilience, ID locale content

## Deploy
- https://web.koinaku.com (Vercel production, aliased)

## Gates
- `pnpm type-check`: clean
- `pnpm test`: 370 passed / 5 skipped
- `pnpm build`: clean

## Linear
- KO-69 created and marked Done.

## Next recommended task
- Translate `content_variants` example and question bodies to Indonesian so ID users get rich alternate examples/questions instead of falling back to generic true/false.
